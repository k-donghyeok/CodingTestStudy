손님 객

주체지향??

객체지행 프로그래밍의 장점
-블록 형태의 모듈화된 프로그래밍
	-신뢰성 높은 프로그래밍이 가능
	-추가.수정.삭제 가 용이하다
	-재사용성이 높다

객체지향 프로그래밍이 가장좋은 프로그래밍 방식은 아니다 프로젝트? 의 목적에 따라 맞는 방식을 사용해야한다


현실의 객체가 갖는 속성과 기능은 추상화 되어 클래스에 정의된다
클래스는 구체화 되어 프로그램의 객체가 된다.

추상화 과정을 통해 속성과 기능을 뽑아낸다?

추상화의 결과물이 클래스 속성을 멤버변수 기능을 멤버메서드 로 

클래스를 통해 구체화를 시킨 결과물이 객체

현실의 객체를 추상화를 통해 클래스로 만든후 클래스를 구체화하여 객체를 만든다



jvm 의 메모리 구조

Method area
-클래스 정보 처리
	-타입정보
	-Field 정보
	-Method 정보
	-상수 풀

stack
-메서드들의 실행공간
	-thread 별로 별도관리
	-메소드 호출 시 마다 메서드 프레임 적층
	-메서드 프레임에 로컬변수도 쌓이는 구조

heap
-객체를 저장하기 위한 영역
	-theread 에 의해 공유
	-객체가 생성되고 가비지컬렡터 에 의해 정리됨



메소드 area 에 원판이 올라가고 

main 메소드를 먼저 호출하고 stacj 에 main 메소드 공간이 생기고



변수
타입에 따른 분류
기본형
참조형

선언위치에 따른 분류

멤버영역
	

지역변수
	클래스 영역 에 static -> 클래스 멤버 변수
		static 이란? -> (언젠가 변경될수있지만 거의 변경되지 않음)클래스에 하나만 존재하고 공유됨   고정->(final)
		변수의 생성 : 클래스 로더에 의해 클래스가 로딩될때 구성 개별 객체 생성과 무관하며 모든 객체가 공유함
		변수의 초기화 : 타입별로 defalut 초기화후 명시적으로 할당된값으로 초기화
		변수의 접근 : 객체의 생성과 무관하게 클래스이름으로 접근(static way)
		소멸시점 : 클래스가 언로드 될 때 함꼐 제거됨



	클래스 영역에 staci x - > 인스턴스(오브젝트) 변수
		변수  생성시기 객체가 만들어 질때 heap 영역에 객체 별로 생성됨
		변수 초기화 : 타입 별로 default 초기화 -> 명시적 할당괸 값으로 초기화 ->초기화 블록 ->생성자에 의해 초기화
		변수 접근 : 객체 생성후(메모리에 올린 후) 객체 이름 으로 접근
		소멸시점 : 가비지 컬렉터가 알아서 프로그래머가 명시적으로 소멸시킬수없음
		

	함수 내부 -> 지역변수(파라미터 변수)
		 변수 생성시기 : 선언된 라인이 실행될때
		변수 초기화 : 사용하기 전 명시적 초기화 필요
		변수 접근 : 외부에서는 접근이 불가능 
		소멸시점 : 중괄호 벗어 날때
	함수 내부 -> 



문자열 풀 ?? 

메서드
현실 객체의 동작을 프로그래밍 화
	사용이유 : 중복코드 작성을 하지않기위해


메서드를 이용해 모듈화 해보자 

메서드를 누구한테 만들어 주는지가 중요함!!!!!!!!!!!!!!!!!!!!!!!

메서드의 리턴타입은 하나만 적용가능 -> 여러 데이터를 넘기려면 어떻게 해야할까? -> 배열로 반환 (근데 여러가지 데이터가 타입이 다르다면? ) -> 클래스를 만들어서 반환

variable arguments 매개변수의 갯수가 몇개가 올지 모르겠을때 ... 을 사용해서 파라미터는 선언하면 호출시 넘겨준 값의 갯수의 따라 자동으로 배열 생성및 초기화


조건문을 이용해서 return 할경우 모든 조건에서 return 필요


class 멤버와 instance 멤버 간의 참조와 호출

호출하려는 멤버가 메모리에 있는가?


메서드 호출시 파라미터로 입력된 값을 복사해서 전달

메서드 오버로딩 때 리턴타입은의미없음

생성자
목적 : 일반멤버 변수의 초기화나 객체 생성시 실행되야 하는 작업정리

기본 생성자

파라미터가 있는 생성자


this 는 객체에 대한 참조 -> static 영역에서 this 사용불가


this()

초기화 블록






